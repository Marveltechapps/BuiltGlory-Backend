import Joi from "joi";
export const idParam = Joi.object({ params: Joi.object({ id: Joi.string().hex().length(24) }).unknown(true) }).unknown(true);
export const listValidator = Joi.object({ query: Joi.object({ page: Joi.number().integer().min(1), limit: Joi.number().integer().min(1).max(100), search: Joi.string().max(100), sort: Joi.string().valid("newest", "oldest"), status: Joi.string().valid("new", "responded", "visit_scheduled", "negotiating", "closed"), assignedTo: Joi.string().hex().length(24), propertyId: Joi.string().hex().length(24), buyerId: Joi.string().hex().length(24) }).unknown(false) }).unknown(true);
export const createValidator = Joi.object({ body: Joi.object({ propertyId: Joi.string().hex().length(24).required(), enquiryTypes: Joi.array().items(Joi.string().max(80)).default([]), preferredContact: Joi.string().valid("phone", "whatsapp", "email").required(), interestType: Joi.string().valid("schedule_visit", "price_negotiation", "more_details").required(), preferredVisitTime: Joi.string().valid("tomorrow_morning", "tomorrow_afternoon", "this_weekend_morning", "this_weekend_afternoon", "custom", null), preferredVisitDate: Joi.date().iso().allow(null), preferredVisitTimeSlot: Joi.string().max(80).allow("", null), additionalMessage: Joi.string().max(1000).allow("", null) }).unknown(false) }).unknown(true);
export const updateValidator = Joi.object({ body: Joi.object({ status: Joi.string().valid("new", "responded", "visit_scheduled", "negotiating", "closed"), assignedTo: Joi.string().hex().length(24), preferredContact: Joi.string().valid("phone", "whatsapp", "email"), notes: Joi.string().max(2000).allow("", null) }).min(1).unknown(false), params: Joi.object().unknown(true) }).unknown(true);
export const statusValidator = Joi.object({ body: Joi.object({ status: Joi.string(), stage: Joi.string(), decision: Joi.string(), notes: Joi.string().allow("", null), reason: Joi.string().allow("", null) }).unknown(true), params: Joi.object().unknown(true) }).unknown(true);