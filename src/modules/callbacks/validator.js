import Joi from "joi";
export const idParam = Joi.object({ params: Joi.object({ id: Joi.string().hex().length(24) }).unknown(true) }).unknown(true);
export const listValidator = Joi.object({ query: Joi.object({ page: Joi.number().integer().min(1), limit: Joi.number().integer().min(1).max(100), search: Joi.string().max(100), sort: Joi.string().valid("newest", "oldest"), status: Joi.string().valid("pending", "called", "resolved", "missed", "rescheduled", "overdue"), assignedTo: Joi.string().hex().length(24), userId: Joi.string().hex().length(24) }).unknown(false) }).unknown(true);
export const createValidator = Joi.object({ body: Joi.object({ source: Joi.string().valid("help_support", "profile_support", "property_detail", "payment", "interior").required(), sourceScreen: Joi.string().max(120).allow("", null), category: Joi.string().valid("property_inquiry", "pricing", "technical_issue", "complaint", "general", "stage_payment", "interior"), propertyId: Joi.string().hex().length(24), reason: Joi.string().max(1000).allow("", null), preferredTime: Joi.date().iso(), bestTimePreference: Joi.string().valid("morning", "afternoon", "evening") }).unknown(false) }).unknown(true);
export const updateValidator = Joi.object({ body: Joi.object({ outcome: Joi.string().valid("answered", "no_answer", "busy", "wrong_number", "callback_later"), notes: Joi.string().max(2000).allow("", null), resolutionNotes: Joi.string().max(2000).allow("", null), preferredTime: Joi.date().iso(), reason: Joi.string().max(500).allow("", null), assignedTo: Joi.string().hex().length(24) }).min(1).unknown(false), params: Joi.object().unknown(true) }).unknown(true);
export const statusValidator = Joi.object({ body: Joi.object({ status: Joi.string(), stage: Joi.string(), decision: Joi.string(), notes: Joi.string().allow("", null), reason: Joi.string().allow("", null) }).unknown(true), params: Joi.object().unknown(true) }).unknown(true);