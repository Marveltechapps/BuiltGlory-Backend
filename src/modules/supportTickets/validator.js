import Joi from "joi";
export const idParam = Joi.object({ params: Joi.object({ id: Joi.string().hex().length(24) }).unknown(true) }).unknown(true);
export const listValidator = Joi.object({ query: Joi.object({ page: Joi.number().integer().min(1), limit: Joi.number().integer().min(1).max(100), search: Joi.string().max(100), sort: Joi.string().valid("newest", "oldest"), status: Joi.string().valid("open", "in_progress", "resolved", "closed"), priority: Joi.string().valid("low", "medium", "high", "urgent"), assignedTo: Joi.string().hex().length(24), userId: Joi.string().hex().length(24) }).unknown(false) }).unknown(true);
export const createValidator = Joi.object({ body: Joi.object({ category: Joi.string().valid("property_inquiry", "payment", "technical", "kyc", "general", "complaint").required(), subject: Joi.string().max(200).required(), message: Joi.string().max(5000).required(), priority: Joi.string().valid("low", "medium", "high", "urgent"), attachments: Joi.array().items(Joi.string()) }).unknown(false) }).unknown(true);
export const updateValidator = Joi.object({ body: Joi.object({ message: Joi.string().max(5000), resolve: Joi.boolean(), resolutionResponse: Joi.string().max(5000), status: Joi.string().valid("open", "in_progress", "resolved", "closed"), priority: Joi.string().valid("low", "medium", "high", "urgent"), assignedTo: Joi.string().hex().length(24), escalation: Joi.object({ targetAssignee: Joi.string().hex().length(24).required(), reason: Joi.string().max(1000).required() }) }).min(1).unknown(false), params: Joi.object().unknown(true) }).unknown(true);
export const statusValidator = Joi.object({ body: Joi.object({ status: Joi.string(), stage: Joi.string(), decision: Joi.string(), notes: Joi.string().allow("", null), reason: Joi.string().allow("", null) }).unknown(true), params: Joi.object().unknown(true) }).unknown(true);